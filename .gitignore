# macOS
.DS_Store

# Xcode user data — never commit
**/xcuserdata/
**/*.xcuserstate

# Build products
build/
DerivedData/
*.xcarchive

# Local overrides
.env

# Runtime GPX — rewritten on every spoof.sh run
GPSSpoof/GPSSpoof/locations/target.gpx
